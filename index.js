import getPosts , {getPostsLength} from "./postController.js";

console.log(getPosts());
console.log(getPostsLength());

// const {generateRandomNumbers, celciusToFaranHeight} = require('./utils');

// console.log(`randomNumbers:${generateRandomNumbers()}`);
// console.log(`celcius to FH:${celciusToFaranHeight(0)}`);